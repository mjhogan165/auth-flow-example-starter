import { Router } from "express";

const userController = Router();

// todo
// Needs _____?
userController.patch("/users/:userId", (req, res) => {});

// todo
// Needs _____?
userController.get("/users/:userId/dogs", (req, res) => {});
